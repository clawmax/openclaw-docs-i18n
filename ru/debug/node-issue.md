

  Среда и отладка

  
# Сбой Node + tsx

На этой странице описано устранение сбоев **Node.js и tsx** при запуске шлюза или CLI OpenClaw (например, `node --import tsx` или скриптов с tsx).

## Типичные причины

- **Версия Node** : для OpenClaw требуется Node 22+. Более старая версия может вызывать сбои среды выполнения или нативных модулей.
- **tsx / загрузчик** : сбои при старте или загрузке TypeScript могут быть из‑за версии tsx, порядка `--import` или конфликтующих загрузчиков.
- **Память / нативное** : большая нагрузка или нативные аддоны могут приводить к OOM или segfault; см. [Node.js](../install/node.md) по версии и окружению.

## Быстрая проверка

1. **Версия Node** : выполнить `node -v` (ожидается v22 или выше).
2. **Воспроизведение без tsx** : попробовать тот же сценарий с обычным `node` и скомпилированным JS, чтобы понять, связан ли сбой с tsx.
3. **Диагностика** : использовать [флаги диагностики](../diagnostics/flags.md) (например, `OPENCLAW_DEBUG_*`) для получения дополнительных логов перед сбоем.

## См. также

- [Отладка](../help/debugging.md) — переопределения runtime, режим watch шлюза, профиль dev
- [Скрипты](../help/scripts.md) — вспомогательные скрипты и соглашения
- [Node.js](../install/node.md) — версия Node и PATH
- [Флаги диагностики](../diagnostics/flags.md) — флаги отладки и трассировки
