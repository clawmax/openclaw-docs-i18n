

  Команды CLI

  
# reset

Сбросить локальную конфигурацию/состояние (CLI остается установленным).

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```

[qr](./qr.md)[Sandbox CLI](./sandbox.md)