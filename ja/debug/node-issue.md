

  環境とデバッグ

  
# Node + tsx クラッシュ

このページでは、OpenClaw ゲートウェイまたは CLI 実行時（例：`node --import tsx` や tsx を使うスクリプト）の **Node.js と tsx** のクラッシュのトラブルシューティングを説明します。

## 主な原因

- **Node バージョン**：OpenClaw は Node 22+ を想定しています。それより古い Node ではランタイムやネイティブモジュールのクラッシュの原因になります。
- **tsx / ローダー**：起動時や TypeScript 読み込み時のクラッシュは、tsx のバージョン、`--import` の順序、競合するローダーが原因のことがあります。
- **メモリ / ネイティブ**：負荷が大きい処理やネイティブアドオンで OOM やセグフォが発生することがあります。バージョンと環境については [Node.js](../install/node.md) を参照してください。

## 簡易チェック

1. **Node バージョン**：`node -v` を実行（v22 以上であること）。
2. **tsx なしで再現**：同じ処理をプレーンな `node` と事前コンパイルした JS で実行し、クラッシュが tsx 固有か確認します。
3. **診断**：クラッシュ前に [診断フラグ](../diagnostics/flags.md)（例：`OPENCLAW_DEBUG_*`）でログを増やします。

## 関連

- [デバッグ](../help/debugging.md) — ランタイム上書き、ゲートウェイ watch、開発プロファイル
- [スクリプト](../help/scripts.md) — ヘルパースクリプトと慣例
- [Node.js](../install/node.md) — Node バージョンと PATH
- [診断フラグ](../diagnostics/flags.md) — デバッグ・トレースフラグ
